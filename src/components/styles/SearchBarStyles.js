import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    margin: 16,
    borderRadius: 25,
    padding: 2,
  },
  input: {
    height: 45,
    backgroundColor: '#ffffff33',
    borderRadius: 25,
    paddingHorizontal: 16,
    fontSize: 16,
    color: '#fff',
  },
});
// This is the style for the search bar component.